[phases.setup]
nixPkgs = ["python311", "solc"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "pip install slither-analyzer"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
